<div class = "comment" >
	<p><%= comment.body %></p>
	<p><%= comment.user.email %></p>
	<div class = "info">
			<%= distance_of_time_in_words(comment.created_at, Time.now) %>
	</div>
	<p><%= link_to "Delete This Comment?", comment, method: :delete, data: { confirm: 'Are you sure?' } %></p>
</div>